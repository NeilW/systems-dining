#!/bin/sh
set -e
if [ "$#" -gt 1 ]
then
    echo "Usage: ${0##*/} [username]" >&2
    exit 1
fi

if [ "$#" -eq 1 ]
then
    uidfilter=_UID=$(id -u "$1")
fi
exec journalctl SYSLOG_FACILITY=16 ${uidfilter}
