#!/bin/sh
set -e
echo "Finished eating."
if /usr/bin/find /home/share/dining-room/forks/ -type f '!' -perm -1000 -writable -exec chmod a+t '{}' '+' 2>/dev/null
then
    echo "Forks for seat $(my-dining-seat) are sticky"
else
    echo "Dining room has closed"
fi
exit 0
